

// we will be defining fonts
// each font is either a class, or there is a class
// that can load a font specification?

// who knows.
// fonts define grids for letters.
// fonts define shapes and sites at which shapes are drawn


// fonts will take inputs.
// the inputs modify the letter-grid
// I'm sure there is a name for it.

// the font takes a page, a mark on the page, a starting
// parameter on the mark, and a letter (which letter to draw),
// and draws a letter

// so, we need fonts, letter grids, marks,
// letter grids are defined relative to page size? 